$(document).ready(function() {
  // This is called after the document has loaded in its entirety
  // This guarantees that any elements we bind to will exist on the page
  // when we try to bind to them

  // See: http://docs.jquery.com/Tutorials:Introducing_$(document).ready()





  // $('.question-field-wrapper').each(function() {
  //     var $question_wrapper = $('.question-fields', this);
  //     $(".add-field", $(this)).click(function(e) {
  //         $('.question-field:first-child', $question_wrapper).clone(true).appendTo($question_wrapper).find('input').val('').focus();
  //     });
  //     $('.question-field .remove-field', $question_wrapper).click(function() {
  //         if ($('.question-field', $question_wrapper).length > 1)
  //             $(this).parent('.question-field').remove();
  //     });

  //   });

  // $('.choice-field-wrapper').each(function() {
  //     var $choice_wrapper = $('.choice-fields', this);
  //     $(".add-field", $(this)).click(function(e) {
  //         $('.choice-field:first-child', $choice_wrapper).clone(true).appendTo($choice_wrapper).find('input').val('').focus();
  //     });
  //     $('.choice-field .remove-field', $choice_wrapper).click(function() {
  //         if ($('.choice-field', $choice_wrapper).length > 1)
  //             $(this).parent('.choice-field').remove();
  //     });
  // });



});
